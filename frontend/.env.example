# Frontend Environment Variables

# Backend base URL (no trailing slash, no /api suffix — the app appends paths).
# Leave unset for local development: defaults to http://localhost:4006
VITE_API_URL=
# Deployed example (Render): VITE_API_URL=https://grant-automation-api.onrender.com
